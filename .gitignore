.DS_Store
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata
*.xcodeproj/xcuserdata
.idea/
Package.resolved
.claude/scheduled_tasks.lock
.claude/scheduled_tasks/
build/
# User data / secrets — guard against committing them if a workspace or the
# app's config ever lives inside the repo tree (privacy).
llm_config.json
*.outputs.json
.*.outputs.json
.*.sheet.json

# irr (MCP semantic index) — local cache, not tracked
.irr/
